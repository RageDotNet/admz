# Notes for AI assistants

- **Do not number markdown section headings** (e.g. `## 3. Storage`). Numbered sections require tedious renumbering cascades on every later insert/remove. Use plain descriptive headings and refer to sections by name (e.g. see "Storage").
- Only the closing section convention: title it "Beyond the scope of this document".
