admins:
  - username: admin
    password: "changeme"  # replace with a pbkdf2:sha256 hash in production (#23)
    token: null  # optional admin bearer token (dmzadm_...)
secret_key: "dev-secret-change-me"
database_url: sqlite:///data/dmz.db
# Arbiter LLM via OpenRouter; the key stays in .env (gitignored) as
# OPENROUTER_API_KEY and is loaded by admz.core.config via python-dotenv.
arbiter_model: "openrouter/openai/gpt-4o-mini"
flask_debug: true
#host: "0.0.0.0"
# Agent key body length: random base64url chars before the 2-char checksum (default 14 → dmz_ + 16).
#key_payload_chars: 14

